{
  "name": "cotizador-viajes",
  "version": "1.0.0",
  "private": true,
  "description": "Prototipo del Sistema de Cotización de Viajes - Equipo 6",
  "scripts": {
    "instalar": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n servidor,cliente -c blue,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server""postinstall": "npm install --prefix server && npm install --prefix client",
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}